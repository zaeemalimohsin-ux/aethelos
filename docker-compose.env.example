# Generated by Start-AethelOS.ps1 — optional overrides only.
# Same-origin relay (/ws) is resolved at runtime; usually leave these empty.

VITE_DEFAULT_RELAY_URL=
VITE_BOOTSTRAP_RELAYS=
VITE_INVITE_BASE_URL=
